"""
# Fine-Tuning YOLOv8 для детекции археологических объектов

Этот проект предоставляет полный набор инструментов для дообучения (fine-tuning) модели YOLOv8 на кастомном датасете для обнаружения археологических объектов с FPV-дронов.

---

## 🚀 Быстрый старт

```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Подготовка датасета (пример)
# (поместите ваши изображения в `raw_data/images` и аннотации в `raw_data/labels`)
python3 scripts/prepare_dataset.py split \
    --images raw_data/images \
    --labels raw_data/labels \
    --output dataset

# 3. Начало обучения
python3 scripts/train.py --config configs/training_config.yaml

# 4. Валидация лучшей модели
python3 scripts/validate.py --model results/archaeological_detection/weights/best.pt

# 5. Применение модели к новым изображениям
python3 scripts/predict.py \
    --model results/archaeological_detection/weights/best.pt \
    --source /path/to/new/images
```

---

## 📂 Структура проекта

```
yolov8_finetuning/
├── configs/                    # Конфигурационные файлы
│   ├── archaeological_dataset.yaml  # Конфиг датасета
│   └── training_config.yaml       # Конфиг обучения
├── dataset/                    # Подготовленный датасет
│   ├── images/
│   │   ├── train/
│   │   ├── val/
│   │   └── test/
│   └── labels/
│       ├── train/
│       ├── val/
│       └── test/
├── models/                     # Обученные модели (опционально)
├── predictions/                # Результаты применения модели
├── raw_data/                   # Исходные данные (создается пользователем)
│   ├── images/
│   └── labels/
├── results/                    # Результаты обучения
│   └── archaeological_detection/
│       ├── weights/            # Веса модели (best.pt, last.pt)
│       ├── results.png         # Графики обучения
│       └── ...
├── scripts/                    # Скрипты
│   ├── prepare_dataset.py    # Утилиты для датасета
│   ├── train.py              # Скрипт обучения
│   ├── validate.py           # Скрипт валидации
│   └── predict.py            # Скрипт для предсказаний
├── requirements.txt            # Зависимости
└── README.md                   # Эта документация
```

---

## ⚙️ Шаг 1: Подготовка датасета

### 1.1. Сбор данных

- **Изображения:** Соберите изображения с дронов, содержащие археологические объекты. Сохраните их в формате `.jpg` или `.png`.
- **Аннотации:** Для каждого изображения создайте текстовый файл `.txt` с таким же названием. Каждая строка в файле должна описывать один объект в формате YOLO:

  ```
  <class_id> <x_center> <y_center> <width> <height>
  ```

  - `<class_id>`: индекс класса (начиная с 0).
  - Координаты нормализованы (от 0 до 1).

### 1.2. Структура исходных данных

Создайте директорию `raw_data` и поместите в нее ваши данные:

```
raw_data/
├── images/
│   ├── img1.jpg
│   ├── img2.jpg
│   └── ...
└── labels/
    ├── img1.txt
    ├── img2.txt
    └── ...
```

### 1.3. Разделение датасета

Используйте скрипт `prepare_dataset.py` для разделения данных на обучающую, валидационную и тестовую выборки:

```bash
python3 scripts/prepare_dataset.py split \
    --images raw_data/images \
    --labels raw_data/labels \
    --output dataset \
    --train-ratio 0.7 \
    --val-ratio 0.2 \
    --test-ratio 0.1
```

Эта команда создаст директорию `dataset` с необходимой структурой.

### 1.4. Анализ датасета

Вы можете проанализировать распределение классов и другие параметры датасета:

```bash
python3 scripts/prepare_dataset.py analyze \
    --dataset dataset \
    --config configs/archaeological_dataset.yaml
```

### 1.5. Визуализация аннотаций

Проверьте корректность аннотаций, визуализировав их на изображениях:

```bash
python3 scripts/prepare_dataset.py visualize \
    --images dataset/images/train \
    --labels dataset/labels/train \
    --output results/annotations_visualization \
    --config configs/archaeological_dataset.yaml \
    --num-samples 20
```

---

## 🧠 Шаг 2: Обучение модели

### 2.1. Конфигурация

- **`configs/archaeological_dataset.yaml`**: Укажите пути к вашему датасету и названия классов.
- **`configs/training_config.yaml`**: Настройте параметры обучения (количество эпох, размер батча, модель и т.д.).

### 2.2. Запуск обучения

Для запуска обучения выполните команду:

```bash
python3 scripts/train.py --config configs/training_config.yaml
```

Вы можете переопределить любые параметры из конфига через командную строку:

```bash
# Обучение на 50 эпох с батчем 8
python3 scripts/train.py --epochs 50 --batch 8

# Продолжение обучения с последнего чекпоинта
python3 scripts/train.py --resume
```

Результаты обучения будут сохранены в директории `results/archaeological_detection`.

---

## 📊 Шаг 3: Валидация модели

После обучения оцените качество лучшей модели на валидационной или тестовой выборке:

```bash
# Валидация на валидационной выборке
python3 scripts/validate.py \
    --model results/archaeological_detection/weights/best.pt \
    --data configs/archaeological_dataset.yaml \
    --split val

# Валидация на тестовой выборке
python3 scripts/validate.py \
    --model results/archaeological_detection/weights/best.pt \
    --split test
```

Скрипт выведет подробные метрики (mAP, Precision, Recall) для каждого класса и сохранит результаты в `validation_results.json`.

---

## 🎯 Шаг 4: Применение модели (Prediction)

Используйте обученную модель для обнаружения объектов на новых изображениях или видео.

```bash
python3 scripts/predict.py \
    --model results/archaeological_detection/weights/best.pt \
    --source /path/to/your/data \
    --conf 0.6 \
    --project predictions \
    --name my_first_prediction
```

- `--source`: может быть путем к изображению, видео или папке с изображениями.
- `--conf`: порог уверенности для детекции.

Результаты будут сохранены в `predictions/my_first_prediction`.

---

## 🛠️ Утилиты для датасета

Скрипт `scripts/prepare_dataset.py` предоставляет несколько полезных команд:

- **`split`**: Разделение датасета.
- **`analyze`**: Анализ и статистика.
- **`visualize`**: Визуализация аннотаций.
- **`convert`**: Конвертация из формата COCO в YOLO.

  ```bash
  # Пример конвертации COCO -> YOLO
  python3 scripts/prepare_dataset.py convert \
      --coco-json /path/to/coco.json \
      --images /path/to/images \
      --output /path/to/yolo_labels
  ```

---

## 💡 Советы и рекомендации

- **Выбор модели:** Начните с `yolov8n.pt` (nano) для быстрого прототипирования. Для большей точности используйте `yolov8s.pt` (small) или `yolov8m.pt` (medium), но они требуют больше ресурсов.
- **Размер батча:** Если вы получаете ошибку `CUDA out of memory`, уменьшите размер батча (`--batch`).
- **Аугментация:** Настройки аугментации в `training_config.yaml` важны для улучшения обобщающей способности модели. Экспериментируйте с ними.
- **Early Stopping:** Параметр `patience` в конфиге обучения позволяет автоматически остановить обучение, если качество модели перестало улучшаться, что экономит время.

---

## 📄 Зависимости

Все необходимые библиотеки перечислены в `requirements.txt`.

```
ultralytics
opencv-python-headless
matplotlib
numpy
PyYAML
```

Установите их с помощью:

```bash
pip install -r requirements.txt
```
"""
